# Starting project = frontend (nextjs) + backend (payload cms) (headless CMS)

## In the begining:

- turbopack is installed
- I use react 19. 
- I use tailwindcss v4
- I use Nextjs 16+

## Which packages has been installed (frontend):

- React Hook Form
- Sass
- typescript
- tailwind
- qs
- shadcn
- stripe
- plaiceholder
- swiper
- zod
- react-photoswipe-gallery
- photoswipe

## Branch structure:

- master: main branch, you can push all of the changes which is ready/finished
- preview: preview branch, this branch is good in cases like: I have project in the internet (production) and i want check how something will looks in the internet
- develop: this branch is using to working on creating some section, things etc. just work

E.g.: You are working on develop branch and finished some section, so you can push changes to master (but i prefer to push to *preview*) branch

## Files structures:

1. main:

   - backend – for Payload CMS (CMS)
   - frontend-new – for Next.js (frontend application)

2. frontend:
   - src/
     - app/
       - assets/: project assets (e.g. images, for section on mainpage and subpages)
         - subpages/: assets for subpages (e.g. error, 404)
       - components/: main page components (e.g. components for mainpage)
       - UI/: UI components used multiple times (e.g. buttons, dialogs, mobile navigation)
       - styles/: global styles and SCSS/CSS variables
         - \_colors.scss: color variables
         - \_components.scss: reusable classes
         - \_functions.scss: SCSS functions
         - \_variables.scss: SCSS variables
         - globals.css: global styles (reset styles)
       - utils/: tools and helper functions for the app layer
       - icons/: SVG/TSX icons
       - constants/: project constants (e.g. accordionData.ts or navigationData.ts)
       - api/: API request handling
       - (subpages)/: project subpages
         - podstrona/: example subpage
           - page.tsx: subpage file
             Structure of subpages:
             subpage-name (folder):
             - page.tsx
             - subpage-name.module.scss: I am using the same name like folder name but in english, not page.module.scss
             - /components (folder, if required)
       - page.tsx: main page
       - layout.tsx: main layout
       - not-found.tsx, error.tsx: error handling
       - favicon.ico, 404.module.scss: favicon and 404 styles
     - componentsShadc/: UI components generated by shadcn (e.g. button, dialog, navigation-menu, etc.)
     - utils/: general utilities (e.g. fetch-api, get-strapi-url, getImageWithBlur, types)
     - lib/: helper functions (e.g. stripe, utils, currency conversions)
     - data/: data files (e.g. loaders.ts)

## Dynamic data from CMS

If the project will be static i can use `Metadata` not `generateMetadata` :)

Example of getting data from CMS to page:

1. define loader:

   ```
   async function loader() {

   try {
   	const data = await getHomePage()
   	if(!data || !data.data) {
		throw new Error("Błąd pobierania danych dla strony głównej")
   	}
   	return { ...data.data }

   } catch (error) {
   	console.error('Błąd pobierania danych dla strony głównej:', error)
   	throw error
   	}
   }

   ```

2. generateMetadata:

   ```
   export async function generateMetadata(): Promise<Metadata> {

   try {
      const homepage: MetadataProps = await loader()
      if (!homepage) {
      return {
         title: 'Strona główna',
         description:
         'Domyślny opis jeśli danych nie będzie',
      }
   }
   return {
         title: `${homepage.tytul} - Nazwa firmy (nie trzeba ale można)`,
         description: homepage.opis,
         keywords: homepage.slowaKluczowe,
      }
   } catch (error) {
      console.error('Błąd generowania metadata dla strony głównej:', error)
      return {
         title: 'Strona główna',
         description:
         'Domyślny opis jeśli danych nie będzie',
         }
      }
   }

   ```

3. Displaying blocks from CMS on the page:
   ```
   export default async function Home() {
   	const data = await loader()
   	const blocks = data?.bloki || []
   	return (
   		<>
   			<MainContent style={{ overflow: 'hidden', marginTop: `${navMarginTop}` }}>
   				<BlockRenderer blocks={blocks} />
   			</MainContent>
   		</>
   	)
   }
   ```
4. Display data from CMS on the page:
   1) define interface for page (if this is a section define in /utils/types.ts otherwise I can define in component but it will be better if i hold interfaces in one file - types.ts):
      ```
      interface ExampleComponentProps {
	      tytul: string
	      tekst: string
      ...
      }
      
      export default async function ExampleComponent(){
    	const {tytul, tekst}:ExampleComponentProps = await loader() (I dont have to destructure data)
      }
    |or|
   
    ```
    export default async function ExampleComponent(){
    	const data = await loader()
    }
	```
	|or|
	
	`export default async function BlockComponent({tytul, tekst}: ExampleComponentProps) {...}`

### Block rendering:

`<BlockRenderer blocks={bloki_z_cms}>`

BlockRenderer.tsx:
```
function blockRenderer(block: Block, index: number) {
	switch (block.__component) {
		case 'bloki.sekcja-hero': - nazwa sekcji musi być taka sama jak w Block /types!
			return <FirstSection {...block} key={index} />
        case 'bloki.inna-sekcja:
            return <InnaSekcja {...block} key={index}>
		default:
			return null
	}
}
```

## Steps to creating Blocks (type):

/utils/types.ts

1. `type ComponentType = | 'bloki.sekcja-hero' | 'bloki.inna-sekcja'` (the name of block must be the same as the response from CMS, postman!)
2. `export type Block = | HeroSectionProps | InnaSekcja |` here add the interface name of section
3. create interface for section:
   ```
   export interface HeroSectionProps extends Base<'bloki.sekcja-hero'> {
   	tytul: string
   	and another necessary things
   }
   
   export interface InnaSekcja extends Base<'bloki.inna-sekcja'> {...}
   ```

## Steps to creating query by qs:

/data/loaders.ts

1. define query - use interactive query builder from strapi
   ```
   const homePagequery = qs.stringify({
   	populate:{...}
   })
   ```

2. define data retrieval function:

   ```
   export async function getHomePage() {
	   const path = '/api/strona-glowna'
	   const BASE_URL = getStrapiURL()
	
	   const url = new URL(path, BASE_URL)
	   url.search = homePageQuery
	
	   return fetchAPI(url.href, {method: "GET"})
   }
   ```
## How to use <PaginationComponent />

   ```

   export default async function News({ searchParams }: PageProps) {
	   const params = await searchParams
	   const currentPage = parseInt(params.page || '1')
	   const { posts: newsPosts, pagination } = await loaderPosts(currentPage)

      
      const typedNewsPosts: NewsPostsProps[] = newsPosts as NewsPostsProps[]

      return (
         {pagination && typedNewsPost.length > 0 && (
            <PaginationComponent currentPage={currentPage} totalPages={pagination.pageCount} />
         )}
      )
   }
   ```

## WARNINGS!
- I cant use <StrapiImage /> in client components (because of getImageWithBlur)

### Todo:
---
